# Solid Cache

* メッセージをテーブルに保存し、そのテーブルを専用のスレッドでポーリングしている
  * 更新があったら、メッセージ送信
* メッセージ送信時に、古いメッセージを必要に応じてジョブで実行
  * `perform_now`を使って即時実行
  * データ量が多い場合、負荷が掛かる可能性がある為、そういう環境でアプリ側でジョブを非同期で明示的に実行してくれ、というスタンス
